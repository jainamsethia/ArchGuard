[tool.poetry]
name = "archguard"
version = "0.3.0"
description = "Architectural drift detector for Python codebases."
authors = []
readme = "README.md"
packages = [{include = "archguard"}]
include = [
    {path = "archguard/templates/*"},
    {path = "archguard/py.typed"}
]

[tool.poetry.scripts]
archguard = "archguard.cli.main:main"

[tool.poetry.dependencies]
python = "^3.11"
typer = ">=0.9,<1.0"
rich = ">=13.0"
pyyaml = ">=6.0"
jsonschema = ">=4.17"
ruamel-yaml = ">=0.18"
tree-sitter = ">=0.21,<0.22"
tree-sitter-python = ">=0.21,<0.22"
networkx = ">=3.0"
python-louvain = ">=0.16"
pydriller = ">=2.5"
httpx = ">=0.25"
watchdog = ">=3.0.0"
anthropic = {version = ">=0.109.0,<0.112.0", optional = true}
pydantic = ">=2.0,<3.0"
numpy = {version = ">=1.24,<2.0.0", optional = true}
scipy = {version = ">=1.10", optional = true}
sentence-transformers = {version = ">=2.2", optional = true}
faiss-cpu = {version = ">=1.7", optional = true}
boto3 = {version = ">=1.28.0", optional = true}
fastapi = {version = ">=0.100.0", optional = true}
uvicorn = {version = ">=0.23.0", optional = true}
cachetools = ">=5.0"

[tool.poetry.extras]
ml = ["numpy", "scipy", "sentence-transformers", "faiss-cpu"]
cloud = ["anthropic", "boto3"]

dashboard = ["fastapi", "uvicorn", "python-multipart"]
all = ["numpy", "scipy", "sentence-transformers", "faiss-cpu", "anthropic", "boto3", "fastapi", "uvicorn", "python-multipart"]

[tool.poetry.group.dev.dependencies]
pytest = ">=7.0"
pytest-cov = ">=4.0"
pytest-benchmark = ">=4.0"
pytest-asyncio = ">=0.23"
pytest-timeout = ">=2.2"
mypy = ">=1.5"
ruff = ">=0.1"
pre-commit = ">=3.0"
types-PyYAML = "*"
pip-audit = ">=2.6"
fastapi = ">=0.100.0"
build = ">=1.0"
bandit = "^1.9.4"


[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[[tool.mypy.overrides]]
module = [
    "tree_sitter.*",
    "faiss.*",
    "sentence_transformers.*",
    "community.*",
    "pydriller.*",
    "jsonschema.*",
    "networkx.*",
    "boto3.*",
    "scipy.*",
]
ignore_missing_imports = true

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
addopts = "--cov=archguard --cov-report=term --cov-report=xml --cov-fail-under=80 -m 'not integration'"
testpaths = ["tests/unit", "tests/integration"]
markers = [
    "integration: requires external dependencies (real git repo, network, API keys)",
    "slow: marks tests as slow-running",
]
